<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!--
  This file is included in the Next.js standalone output via
  outputFileTracingIncludes so the fontconfig directory is available at runtime.
  The actual font path is written dynamically by configureReportFontconfig()
  in report-chart-service.ts using os.tmpdir().
-->
<fontconfig>
</fontconfig>
